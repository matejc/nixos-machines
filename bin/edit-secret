#!/usr/bin/env bash

machineName="${1?"Missing machine name as first arg!"}"
secretName="${2?"Missing secret name as first arg!"}"
identityFile="${3:-"$HOME/.ssh/id_ed25519"}"

exec nix run ".#encrypt" -- "$machineName" "$secretName" "$identityFile"
