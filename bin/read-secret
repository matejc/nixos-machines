#!/usr/bin/env bash

machineName="${1?"Missing machine name as first arg!"}"
secretName="${2?"Missing secret name as second arg!"}"
identityFile="${3:-"$HOME/.ssh/id_ed25519"}"

exec nix run ".#read-secret" -- "$machineName" "$secretName" "$identityFile"
